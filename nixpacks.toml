[phases.setup]
nixPkgs = ["nodejs-20_x", "python310", "gcc"]

[phases.install]
cmds = [
  "npm install --prefix frontend",
  "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = ["npm run build --prefix frontend"]

[start]
cmd = "python3 backend/serve_prod.py"
